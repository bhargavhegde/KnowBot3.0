export default function TestPage1() { //tsx- typescript xml because it also has html tags. so tsx instead of ts.(html)
    
    return (
        <div>
            <h1>Test Page 1</h1>
        </div>
    );
}